const defaultState = {
    category : '',
    alpha : '',
    singerList : [],
    enterLoading : true,
    listOffest : 0
}

export default (state = defaultStatus , action) =>{
    switch (action){
        default :
            return state
    }
}